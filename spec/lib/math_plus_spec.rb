RSpec.describe PhysicsPlus do
  # it "Heron's formula (3, 4, 5) = 6" do
  #   expect(MathPlus.geron(3, 4, 5)).to eq(6.0)
    
  # end

  # it "Heron's formula (6, 8, 10) = 24" do 
  #   expect(MathPlus.geron(6, 8, 10)).to eq(24.0)
  # end

  # it "Heron's formula (1, 1, 3) = error" do 
  #   expect { MathPlus.geron(1, 1, 3).to }.to raise_error(RuntimeError, "The triangle doesn't exist")
  # end
end
